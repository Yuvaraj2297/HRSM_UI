import { Datepicker } from './datepicker';

describe('Datepicker', () => {
  it('should create an instance', () => {
    const directive = new Datepicker();
    expect(directive).toBeTruthy();
  });
});
